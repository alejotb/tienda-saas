import 'package:flutter/material.dart';

class CheckoutInfoHeader extends StatelessWidget {
  const CheckoutInfoHeader({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
